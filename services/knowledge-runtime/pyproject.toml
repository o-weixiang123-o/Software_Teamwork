[project]
name = "knowledge-runtime"
version = "0.1.0"
description = "Host-run Knowledge document parsing, indexing, retrieval, and rerank runtime."
authors = [{ name = "Software Teamwork contributors" }]
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "anthropic==0.76.0",
    "azure-identity>=1.25.3",
    "azure-storage-file-datalake==12.16.0",
    "beartype>=0.20.0,<1.0.0",
    "boto3>=1.28.0",
    "chardet>=5.2.0,<6.0.0",
    "cn2an==0.5.22",
    "cohere==5.6.2",
    "dashscope==1.25.11",
    "debugpy>=1.8.13",
    "demjson3==3.0.6",
    "editdistance==0.8.1",
    "elasticsearch-dsl==8.12.0",
    "exceptiongroup>=1.3.0,<2.0.0",
    "extract-msg>=0.39.0",
    "ffmpeg-python>=0.2.0",
    "google-cloud-storage>=2.19.0,<3.0.0",
    "google-genai>=1.41.0,<2.0.0",
    "groq>=0.30.0,<1.0.0",
    "grpcio-status==1.67.1",
    "html-text==0.6.2",
    "infinity-sdk==0.7.0",
    "infinity-emb>=0.0.66,<0.0.67",
    "json-repair==0.35.0",
    "mammoth>=1.11.0",
    "markdown>=3.8.1,<4.0.0",
    "markdown-to-json==2.1.1",
    "markdownify>=1.2.0",
    "mcp>=1.19.0",
    "mini-racer>=0.12.4,<0.13.0",
    "minio==7.2.4",
    "mistralai>=2.5.1,<3.0.0",
    "mysql-connector-python>=9.0.0,<10.0.0",
    "mypy-boto3-s3==1.40.26",
    "networkx>=3.6.0,<4.0.0",
    "ollama>=0.5.0",
    "opendal>=0.45.0,<0.46.0",
    "opensearch-py==2.7.1",
    "ormsgpack>=1.6.0",
    "pdfplumber==0.10.4",
    "psycopg2-binary>=2.9.11,<3.0.0",
    "pyobvector==0.2.22",
    "pypandoc>=1.16",
    "pypdf>=6.10.2",
    "python-calamine>=0.4.0",
    "python-docx>=1.1.2,<2.0.0",
    "paramiko>=3.5.1",
    "python-pptx>=1.0.2,<2.0.0",
    "qianfan==0.4.6",
    "quart-auth==0.11.0",
    "quart-cors==0.8.0",
    "ranx==0.3.20",
    "readability-lxml>=0.8.4,<1.0.0",
    "replicate==0.31.0",
    "reportlab>=4.4.1",
    "roman-numbers==1.0.2",
    "ruamel-base==1.0.0",
    "ruamel-yaml>=0.18.6,<0.19.0",
    "socksio==1.0.0",
    "sqlglotrs==0.9.0",
    "tencentcloud-sdk-python==3.0.1478",
    "tika==2.6.0",
    "valkey==6.0.2",
    "volcengine==1.0.194",
    "voyageai==0.2.3",
    "word2number==1.1",
    "setuptools>=78.1.1,<81.0.0",
    "xxhash>=3.5.0,<4.0.0",
    "xpinyin==0.7.6",
    "peewee>=3.17.1,<4.0.0",
    #    following modules aren't necessary
    #    "nltk==3.9.1",
    #    "numpy>=1.26.0,<2.0.0",
    #    "openai>=1.45.0",
    #    "openpyxl>=3.1.0,<4.0.0",
    #    "pandas>=2.2.0,<3.0.0",
    #    "pillow>=12.2.0,<13.0.0",
    #    "protobuf==5.27.2",
    #    "pymysql>=1.1.1,<2.0.0",
    #    "python-dotenv==1.0.1",
    #    "python-dateutil==2.8.2",
    #    "Quart==0.20.0",
    #    "requests>=2.32.3,<3.0.0",
    #    "scikit-learn==1.5.0",
    #    "selenium==4.22.0",
    #    "shapely==2.0.5",
    #    "six==1.16.0",
    #    "tabulate==0.9.0",
    #    "tiktoken==0.7.0",
    #    "umap_learn==0.5.6",
    #    "werkzeug==3.0.6",
    #    "trio>=0.17.0,<0.29.0",
    #    "click>=8.1.8",
    # 1.88.0 ships a broken `litellm.integrations.custom_logger`
    # (imports `AgenticLoopPlan` from a sibling types module where the
    # symbol is missing — see PR #15952 CI run 27414495913). Security
    # advisories require at least 1.84.0, so keep the safe floor while
    # avoiding that known-bad release.
    "litellm>=1.84.0,<1.88.0",
    #    "pip>=25.2",
    #    "imageio-ffmpeg>=0.6.0",
    #    "cryptography==46.0.3",
    #    "jinja2>=3.1.0",
    "quart-schema==0.23.0",
    "aiohttp>=3.14.1",
]

[dependency-groups]
worker = [
    "crawl4ai>=0.9.0,<0.9.1",
    "en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl",
    "graspologic @ git+https://gitee.com/infiniflow/graspologic.git@38e680cab72bc9fb68a7992c3bcc2d53b24e42fd",
    "onnxruntime==1.23.2; sys_platform == 'darwin' or platform_machine != 'x86_64'",
    "onnxruntime-gpu==1.23.2; sys_platform != 'darwin' and platform_machine == 'x86_64'",
    "opencv-python==4.10.0.84",
    "opencv-python-headless==4.10.0.84",
    "openpyxl>=3.1.5,<4.0.0",
    "pyclipper>=1.4.0,<2.0.0",
    "selenium-wire==5.1.0",
    "spacy==3.8.14",
    "webdriver-manager==4.0.1",
    "xgboost==1.6.0",
]
test = [
    "hypothesis>=6.132.0",
    "openpyxl>=3.1.5",
    "pillow>=12.2.0,<13.0.0",
    "pytest>=8.3.5",
    "pytest-asyncio>=1.3.0",
    "pytest-xdist>=3.8.0",
    "pytest-cov>=7.0.0",
    "python-docx>=1.1.2",
    "python-pptx>=1.0.2",
    "reportlab>=4.4.1",
    "requests>=2.32.2",
    "requests-toolbelt>=1.0.0",
]

[tool.uv]
constraint-dependencies = [
    "azure-core>=1.38.0",
    "cbor2>=5.9.0",
    "cryptography>=48.0.1",
    "flask>=3.1.3",
    "idna>=3.15",
    "lxml>=6.1.0",
    "lxml-html-clean>=0.4.4",
    "mcp>=1.23.0",
    "nltk>=3.9.4",
    "orjson>=3.11.6",
    "protobuf>=5.29.6,<6.0.0",
    "pydantic-settings>=2.14.2",
    "pygments>=2.20.0",
    "pyjwt>=2.13.0",
    "pypdf>=6.13.3",
    "pytest>=9.0.3",
    "python-dotenv>=1.2.2",
    "requests>=2.33.0",
    "urllib3>=2.7.0",
    "werkzeug>=3.1.6",
    # CVE-2026-30922: Denial of Service via unbounded recursion in ASN.1 decoding (CVSS 7.5 HIGH)
    # pyasn1 < 0.6.3 is vulnerable; pulled in transitively via google-auth / rsa / pyasn1-modules
    "pyasn1>=0.6.3",
    # Python 3.13 added pathlib.PurePath.parser as a public class attribute holding
    # the posixpath/ntpath module. trio<0.26 introspects all Path class attributes to
    # generate async forwards and raises TypeError on any non-callable attribute it
    # encounters (fixed in trio 0.26 by skipping non-callables). Pulled in transitively
    # via selenium-wire -> trio-websocket -> trio.
    "trio>=0.26.0",
]
override-dependencies = [
    # crawl4ai 0.9.0 still caps lxml at <6, but no newer crawl4ai release is
    # available on the configured indexes. lxml 6.1+ preserves the HTML/XML APIs
    # used by crawl4ai and is required to clear the security advisory.
    "lxml>=6.1.0",
    # infinity-sdk 0.7.0 caps pyarrow at <23 while no newer stable SDK release
    # is available. The runtime does not use pyarrow directly; override the
    # transitive cap so Dependabot's patched pyarrow floor can be satisfied.
    "pyarrow>=23.0.1",
]
exclude-dependencies = [
    # crawl4ai>=0.8.6 depends on unclecode-litellm, which installs the same
    # `litellm` package namespace as upstream litellm and can corrupt imports.
    "unclecode-litellm",
]
[tool.setuptools]
packages = [
    'api',
    'deepdoc',
    'rag.graphrag',
    'rag',
]

[tool.ruff]
line-length = 200
exclude = [".venv"]

[tool.ruff.lint]
extend-select = ["ASYNC", "ASYNC1"]
ignore = ["E402"]

[tool.pytest.ini_options]
pythonpath = [
    "."
]

testpaths = ["test"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

markers = [
    "p0: critical priority test cases",
    "p1: high priority test cases",
    "p2: medium priority test cases",
    "p3: low priority test cases",
    "smoke: smoke test cases",
    "auth: authentication UI tests",
    "asyncio: mark test as async",
]

# Test collection and runtime configuration
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
filterwarnings = [
    "error",  # Treat warnings as errors
    "ignore::DeprecationWarning",  # Ignore specific warnings
    "ignore:pkg_resources is deprecated:UserWarning",
]

# Command line options
addopts = [
    "-v",  # Verbose output
    "--strict-markers",  # Enforce marker definitions
    "--tb=short",  # Simplified traceback
    "--disable-warnings",  # Disable warnings
    "--color=yes",  # Colored output
    "-p",
    "no:anyio",  # anyio's pytest plugin conflicts with pytest-asyncio on Py3.13
]


# Coverage configuration
[tool.coverage.run]
# Source paths - adjust according to your project structure
source = [
    # "../../api/db/services",
    # Add more directories if needed:
    "../../common",
    # "../../utils",
]

# Files/directories to exclude
omit = [
    "*/tests/*",
    "*/test_*",
    "*/__pycache__/*",
    "*/.pytest_cache/*",
    "*/venv/*",
    "*/.venv/*",
    "*/env/*",
    "*/site-packages/*",
    "*/dist/*",
    "*/build/*",
    "*/migrations/*",
    "setup.py"
]

[tool.coverage.report]
# Report configuration
precision = 2
show_missing = true
skip_covered = false
fail_under = 0  # Minimum coverage requirement (0-100)

# Lines to exclude (optional)
exclude_lines = [
#    "pragma: no cover",
#    "def __repr__",
#    "raise AssertionError",
#    "raise NotImplementedError",
#    "if __name__ == .__main__.:",
#    "if TYPE_CHECKING:",
    "pass"
]

[tool.coverage.html]
# HTML report configuration
directory = "htmlcov"
title = "Test Coverage Report"
# extra_css = "custom.css"  # Optional custom CSS
